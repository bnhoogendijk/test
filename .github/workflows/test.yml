name: Deploy to S3

on:
  push:
    branches:
      - main
    tags:
      - "*"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Test
        run: |
          echo "joe"
          echo ${GITHUB_REF_NAME}
          echo  ${GITHUB_REF}
